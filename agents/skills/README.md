# Personal skills

Add one directory per reviewed, reusable workflow:

```text
agents/skills/my-workflow/SKILL.md
```

Keep skills generic enough to publish. Do not include client names, internal URLs,
account identifiers, raw prompts, credentials, or unreviewed auto-memory. Run the
`agent` profile again after adding a skill to link it into Codex and Claude.
