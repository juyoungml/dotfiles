# Copy to ~/.config/dotfiles/secrets.env and replace references with your own.
# Commit secret references only if their vault/item metadata is safe to publish.
OPENAI_API_KEY=op://Private/OpenAI/api-key
ANTHROPIC_API_KEY=op://Private/Anthropic/api-key
